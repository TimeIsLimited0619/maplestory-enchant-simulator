---
description: 禁用 agent 自動檢查；改由使用者手動驗證
alwaysApply: true
---

# 不自動檢查

- **不要**主動開瀏覽器、跑 E2E、截圖驗畫面、或「幫你看一眼是否能進場」。
- **不要**為了驗證而啟動 `http.server`／dev server（使用者沒要求時）。
- 實作完成後：簡短說明改了什麼即可，**等使用者手動測**；不要主動做驗證清單跑一遍。
- 仍可做：使用者明確要求的檢查、必要的建置／匯入（如 `npm run import:boss`）、語法錯誤導致無法繼續時的最小修復。
- 與 idle-boss 交接規則一致：改完 bump cache 後交手，不代跑實戰驗證。
