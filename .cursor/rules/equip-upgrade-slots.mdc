---
description: 裝備強化次數限制 — tuc 為 0 不可星力／鐵鎚
globs: js/item.js,js/starforce.js,js/hammer.js,js/main.js
alwaysApply: false
---

# 裝備強化次數限制

## 判定

- 以 WZ `tuc` 匯入後的 `baseMaxUpgradeSlots`（或 `maxUpgradeSlots`）是否 **> 0** 為準。
- 共用 helper：`hasBaseUpgradeSlots(item)`（定義於 `js/item.js`）。

## 行為

裝備 **本身無強化次數**（`tuc === 0`）時：

1. **星力強化**（StarForce）：不可強化、不可自動強化；控制台顯示封鎖提示；左側星力 Tab **disabled**。
2. **強化次數增加**（Hammer）：不可選槌、不可敲槌；控制台顯示封鎖提示；左側鐵鎚 Tab **disabled**。
3. **卷軸**（Scroll）：左側卷軸 Tab **disabled**（因無可捲次數）。已用滿卷軸 **不會** disable 卷軸 Tab。

## Tab 可用性（`main.js` → `isCategoryAvailable`）

| 分頁 | disabled 條件（已放裝備時） |
|------|---------------------------|
| star | 無 base tuc，或已達 maxStar |
| hammer | 無 base tuc，或金＋白槌皆用完 |
| scroll | 無 base tuc |
| 其他 | 不因「已滿」而 disable |

啟動預設分頁為 **none**（`Enchant_none_0.png` 待機背景）。若當前分頁變為不可用，自動切回 none。

## 實作注意

- 新增強化入口時，先呼叫 `hasBaseUpgradeSlots(currentEnchantItem)` 再允許操作。
- UI 需有 `blocked` 狀態（非 idle）：已放裝備但不可用該功能。
- 勿以 `upgradeSlots`（含鐵鎚加成後）判斷；僅看 **原始** `baseMaxUpgradeSlots`。
