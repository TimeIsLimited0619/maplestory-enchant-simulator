---
description: 新職業實作交接（技能／紙娃娃／傷害／常見坑；以精靈遊俠為範本）
globs: js/skill*.js,js/paperdoll*.js,js/weaponTypeMap.js,js/jobChangePanel.js,js/characterCombatPanel.js,js/characterProgression.js,js/idleHunt.js,js/idleBossFight.js,js/uiCharacterInfo.js,index.html
alwaysApply: false
---

# 新職業實作交接

給下一個 agent：做新職業前先讀本規則，對照已完成的 **英雄／法師／精靈遊俠（Mercedes）**，複雜機制先提案再動手。  
驗證習慣見 `no-auto-verify.mdc`：改完 bump `index.html` `?v=` 後交手，**不**代開瀏覽器／代測。

## 推薦流程

1. **盤資料**：技能書 jobId、專屬武器種類碼、紙娃娃是否有對應 action 幀  
2. **給方案**（繁中）：技能分類表、要 override 的玩法差異、紙娃娃／普攻缺口、v1 範圍  
3. **使用者拍板**後再改 code  
4. **實作順序**固定如下 → bump cache → 短交手說明

### 實作順序

| 順序 | 檔案／動作 | 職責 |
|------|------------|------|
| 1 | `js/skillJobData.js`（或 `npm run import:skill` 類腳本） | WZ 技能本體 |
| 2 | `js/skillOverrides.js` | 玩法覆寫（被動化、引導、不可裝備、數值） |
| 3 | `js/skillCatalog.js` | 職業線、`FORCE_ADD_ATTACK_FOLLOWUP_IDS`、面板分頁 |
| 4 | `js/skillModifiers.js` | damPlus 連結、超技 enhance、職業特效減傷等 |
| 5 | `js/skillCombat.js` | 施放路徑：引導／投射／接技／連鎖節奏 |
| 6 | `js/skillChannelCast.js`／`skillBallCast.js`／`skillBuffRuntime.js`／`skillMobStatus.js` | 僅在該職需要時動 |
| 7 | `js/weaponTypeMap.js` | 武器種類→職名／係數／**普攻動作池** |
| 8 | `js/paperdoll.js`＋紙娃娃資料 | 普攻／技能 action；缺幀要有後備 |
| 9 | `js/jobChangePanel.js`／`characterCombatPanel.js`／`characterProgression.js` | 轉職 UI、主屬、AP |
| 10 | `index.html` | 所有改過的 JS `?v=` |

## 傷害管線（新職勿另開捷徑）

所有玩家→怪傷害應匯入同一套：

```
UiCharacterInfo.rollHuntHit / calcAttributeAttack
  →（技能%）熟練度 → 爆擊
  → SkillMobStatus.applyOutgoingDamageMods（帶 isCritical／skillId）
  → IdleHunt.resolveMobHitDamage（OHK + BOSS 血線 cap）
  → 顯示用同一 finalDmg → 扣血
  → IdleBossFight.afterAppliedDamage（粉豆殼等）
```

- **顯示＝實扣**：優先 `IdleHunt.applyPlayerHitToMob`；勿先 `showMobDamage(未 cap)` 再扣血。  
- **技能專屬 BD**：加算進 `(1+dmg+BD+skillBd)`（`rollHuntHit`／`getHuntHitDamage` 的 `skillBdR`），勿事後乘算。  
- **怪物防禦 IED×PDRate：目前關閉**（`resolveMobHitDamage` 不呼叫 `applyMobDefense`）。面板無視不完善前**不要重開**；helper 留在 `uiCharacterInfo.js` 供日後用。  
- **結凍粉碎等狀態 IED**：仍是乘算近似，註解已標明。  
- Hunt／BOSS 的 `onMobStateSync` 契約：傳入＝碰過的 mob，**擊殺只看 `hp<=0`**，不可把清單當必殺（伊修塔爾曾因此秒殺章節怪）。

### Sustain／引導（伊修塔爾類）

- `channelCast.sustain`：每 tick 結算；**有死者**才 `syncMobStateAfterDamage`；BOSS 無死者改 `IdleBossFight.tryPhaseCheck`。  
- 死亡／`IdleBossFight.isBusy`／無法再造成傷害 → 中斷引導。  
- BOSS `afterExternalHits`：優先 touched，勿無故 `getCombatMobs()` 全掃。

## 技能系統注意

### Overrides（`skillOverrides.js`）

- 匯入與 `SkillCatalog` 讀取都會 merge；改完**兩個路徑都吃得到**。  
- 語法：物件屬性間**不可缺逗號**（曾整份 PATCHES 掛掉 → 昇龍 CD 等全部失效）。  
- 常見覆寫：`type: 'passive'`、`equipable: false`、`channelCast`、`cooltime`、`lv2mhp`／`mhpR`、說明 `h`／`desc`。

### 接技／連鎖

- **addAttack 接技**：不可裝備；用 `FORCE_ADD_ATTACK_FOLLOWUP_IDS` + override `equipable: false`；面板顯示「接技」。  
- **damPlus 連結**：寫在 `skillModifiers.js` 的 `DAM_PLUS_LINKS`；實戰走 `applySkillEnhance`。  
- **技能連鎖 2–4**：Mercedes 有最低間隔（對齊光速雙擊）；新職若高頻頭技+連鎖，考慮節流避免每 tick 狂放。

### Buff

- 時效 buff：**瞬開**（`lockMs = 0`，不播 huntSwing 延長施放鎖）— 見 `skillCombat` buff 路徑。

## 紙娃娃／普攻（精靈遊俠踩過的坑）

- 普攻池在 `weaponTypeMap.BASIC_ATTACK_ACTIONS_BY_TYPE`。  
- **雙弩槍**正服是 `shoot1`／`shootF`，但目前 `paperdollData` **常缺 shoot 幀** → 必須有 `swingT1`／`swingO1` 後備，否則 `pickBasicAttackAction` → `stand1`（看起來沒普攻）。  
- `paperdoll.pickBasicAttackAction` 在池空時會再退 swing；新遠程職仍應在 weaponTypeMap **主動加後備**。  
- 引導技用 `playHuntSwingLoop`（如 `dualVulcanLoop`）；結束務必 `stopHuntSwingLoop`。  
- 技能 WZ `actions` 要對得上紙娃娃 action 名；缺幀不要 silently stand。

## 職業接入檢查清單

1. 技能書進 `skillJobData`／Catalog 職業線（1–4 轉 jobId）  
2. 專屬武器種類碼 → `weaponTypeMap`（exclusive jobName、係數、普攻池、預設攻速）  
3. 轉職面板圖示 `jobChangePanel.js`  
4. 主屬／AP／戰鬥面板職業鎖定  
5. 主動／被動／buff／超技分類正確；CD、mobCount、damage% 抽樣對過說明  
6. 接技／最終攻擊／召喚／DoT／狀態若有，接到既有 runtime，勿複製一套傷害  
7. 紙娃娃：stand + 至少一種可播的普攻 action  
8. 狩獵＋BOSS 各打一段（使用者測）；agent 只 bump cache  
9. 新職特有機制寫進本規則「已完成職業摘要」

## 已完成職業摘要（簡）

### 英雄（Warrior）

- 鬥氣、最終攻擊、部分超技；激勵改常駐被動 `1101006`。  
- 滿鬥氣強化形態（狂暴等）見 `resolveComboEnhancedForm`。

### 法師（火毒／冰雷）

- 無 MP → 多數改 HP；魔力增幅類用 `mhpR`／`lv2mhp`。  
- 瞬間移動熟練：面板可顯示但 `equipable: false`，擋位靠 stance。  
- 冰雷結凍／粉碎等走 `skillMobStatus`。

### 精靈遊俠（Mercedes，`231x`）

- 頭技伊修塔爾 `23121000`：`channelCast.sustain`；傷害即時、ball 可純動畫。  
- 接技：騰空踢擊／旋風突進／傳說之槍／旋風月光／憤怒天使等 → addAttack，不可裝備。  
- damPlus：伊修塔爾→獨角獸、傳說→落葉、月光→昇龍+騰空等（見 `DAM_PLUS_LINKS`）。  
- 昇龍 `23101001` CD 10s；可打斷 sustain。  
- 雙弩槍精通：`lv2mhp` flat HP。  
- 減傷類曾做 Mercedes 專用 scale（`skillModifiers`）。  
- 普攻：缺 shoot 幀 → swing 後備（見上）。

### 夜使者（Night Lord，`400`／`410`／`411`／`412`）

- 主屬 LUK／副 DEX；武器拳套（係數 1.75）。無低等 0147：新手仍發劍，戰鬥係數／普攻池／攻速改走職業預設拳套。
- 頭技四飛閃 `4121013`（`replacesSkill` 三飛閃）；刻印 `4100011`／`4120018` 被動，命中上標後下一發打星星彈。
- 影分身 `4111002`：`shadowPartnerR` 額外段 + 紙娃娃 `hunt-clone` 半透明層。
- 爆破鏢 extraSkill 爆炸、挑釁 `4121020` 追擊、絕殺領域召喚 autotick、飛毒殺／出血 DoT、絕對領域 bdR＋防下降。
- v1 不做：5／6 轉、隱身／二段跳／暗影衝刺、短劍線、夜幕印記（2 秒無敵）。

## 效能備註

- `UiCharacterInfo` hunt 戰鬥快取 TTL 約 **64ms**（高頻引導）。  
- Sustain 勿每 tick 全狩獵佇列 sync。  
- 傷害數字堆疊用 `_damageStack` session，避免連鎖搶 index。

## 交手回覆格式（短）

- 職業／武器／主屬一句  
- v1 技能範圍與刻意不做的  
- 改了哪些檔（不必貼大段 code）  
- 提醒使用者硬重整後自測：普攻動畫、頭技、BOSS 轉階、章節清怪

## 溝通習慣

- 先盤 WZ／現有相近職再提案；缺圖／缺幀誠實說。  
- 玩法與正服不同處寫進 `skillOverrides` 並改說明字串。  
- 聚焦相關檔；保持繁中簡短回覆。
